% generatesquare.m
Fs = 1000;                    % sampling frequency (Hz)
t = linspace(0, 2*pi, 3);    % time vector from 0 to 2*pi
x = square(t);                % default 50% duty-cycle square wave

