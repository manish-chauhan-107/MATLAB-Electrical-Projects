%rand function
x = rand(5);
%it creats a 5x5 matrix with random elements
y = rand(5,2);
%it creates a matrix with random elements with 5 rows and 2 columns
z = ones(2,6);
%creates a matrix of 2x6 with only (1) as element
A = zeros(4,7);
%creates a matrix of 4x7 with only (0) as element
size(x);
%it is used to get the size of the matrix