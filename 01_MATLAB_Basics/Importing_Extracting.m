% The table from Import Tool has been loaded and displayed in the script.

% To extract a variable from a table, you can use dot notation.
% var = table.VariableName

% all data can be appended together in a column or row: 
% data.HeightMeters = data.HeightYards*0.9144

% Multiply each element of elements.Density with elements.Volume1.
% Remember to use element-wise multiplication with " .* ".

% elements = sortrows(elements, "Mass"); used to sort least to most
% where to store = sortrows(what data to sort, "Sort accord to what")

% Try extracting the first three rows of the table.
% top3 = elements(1:3,:)